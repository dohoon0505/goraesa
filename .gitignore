# 환경 파일
.env
.env.*

# API 키 — 저장소에 두지 않는다 (서버 환경변수 OPENAI_API_KEY 사용)
keystore.json

# 의존성 / 임시물
node_modules/
.playwright-mcp/

# 전달용 압축본 (저장소에 커밋하지 않음)
*.zip

# Office 임시 잠금 파일
~$*

# 전환 전 React 백업 (로컬 대조/롤백용 — git 추적 제외)
Backup React/
